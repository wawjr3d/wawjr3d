(function($) {

	$.fn.dropdownmenu = function(options) {

		var settings = {
			sub_menu_selector : "ul",
			sub_menu_item_selector : "li"
		};

		if (options) { $.extend(settings, options); }

		return this.each(function() {

			var $this = $(this);

					
		});
	}

})($);